# Read-only access to the registered fluid interactions so the JEI plugin can enumerate them.
public net.neoforged.neoforge.fluids.FluidInteractionRegistry INTERACTIONS # Map<FluidType, List<InteractionInformation>>
